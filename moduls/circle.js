/**
 * Create circle object
 * @param opts
 */
export default class  Circle {
    constructor (x, y, radius, color) {
            this.x = x;
            this.y = y;
            this.radius = radius;
            this.fill = color;
    }
}
